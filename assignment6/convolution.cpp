#include <stdio.h> 
#include <stdlib.h>
#include <vector>

// let's do a 1D implementation first


class Matrix {
    std::vector<std::vector<int>> mat; 
    
}


using Matrix = std::vector<std::vector<float>>;

// input size (H x W x D, e.g. 28 x 28 x 1 input for 1 channel) 
// kernel size (). A convolutional filter will shrink the original dimensions: (H - ())

int main(){
    srand(0);
    

}
